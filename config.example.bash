#!/usr/bin/env bash

# FQDN of the GitLab server
export GITLAB_URL="https://gitlab.example.com"
# PORT that the GitLab server is listening on host machine
export GITLAB_WEB_PORT="8080"
# PORT that sshd of the GitLab server is listening on host machine
export GITLAB_SSH_PORT="2222"
